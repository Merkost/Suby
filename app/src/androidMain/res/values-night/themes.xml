<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash Screen Theme for Dark Mode -->
    <style name="Theme.Suby.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@drawable/suby_logo_white</item>

        <item name="windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
        <item name="windowSplashScreenAnimationDuration">300</item>

        <item name="postSplashScreenTheme">@style/Theme.Suby</item>
    </style>
</resources>